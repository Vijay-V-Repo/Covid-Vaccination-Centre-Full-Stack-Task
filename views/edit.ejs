<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Control</title>
</head>
<body>
  <h1>Edit the Vaccination Centre Details</h1>
  <h2>Vaccination Centres</h2>
  <ul>
    <% centres.forEach(centre => { %>
      <li>
        <%= centre.name %> (Working Hours: <%= centre.workingHours %>, Capacity: <%= centre.capacity %>, Available Slots: <%= centre.availableSlots %>)
        <form action="/admin/deleteCentre/<%= centre._id %>" method="POST">
          <button type="submit">Delete</button>
        </form>
      </li>
    <% }) %>
  </ul>
  <h2>Add a Vaccination Centre</h2>
  <form action="/admin/addCentre" method="POST">
    <label for="name">Name:</label>
    <input type="text" id="name" name="name" required><br>
    <label for="workingHours">Working Hours:</label>
    <input type="text" id="workingHours" name="workingHours" required><br>
    <label for="capacity">Capacity:</label>
    <input type="number" id="capacity" name="capacity" required><br>
    <button type="submit">Add Centre</button>
  </form>
</body>
</html>
